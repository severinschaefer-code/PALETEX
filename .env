# Rename this file to .env and fill in your SMTP credentials
SMTP_HOST=smtp.strato.de
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=info@pavecon.de
SMTP_PASS=S1n5eR150988!

# Where to send incoming requests (comma-separated for multiple recipients)
MAIL_TO=info@pavecon.de

# CORS origins allowed (comma-separated). Use * for development.
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
